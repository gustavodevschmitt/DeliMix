import Image from "next/image";

const highlights = [
	
];

const AboutSection = () => {
	return (
		<section className="mx-auto grid w-full max-w-7xl gap-8 px-5 py-10 md:grid-cols-2 md:items-center">

		</section>
	);
};

export default AboutSection;
